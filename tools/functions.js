/** * Form verification */ function verifyMail(champ) {	var regex = /^[a-zA-Z0-9._-]+@[a-z0-9._-]{2,}\.[a-z]{2,4}$/;	if(!regex.test(champ.value)) {		return false;	}	else {		return true;	}}function verifyZip(champ) {	var regex = /^[0-9-]{4,5}$/;	if(!regex.test(champ.value)) {		return false;	}	else {		return true;	}}function cleanField(champ) {	champ.style.background = "";}function verifyField(champ) {	if (champ.value.length < 1) {		return false;	}	else {		return true;	}}function verifyForm(form){	for (i = 0; i < form.elements.length; i++) {		if(form.elements[i].name == "email") {			if (!verifyMail(form.elements[i])) {				alert("Veuillez insérer un email valide, merci.");				return false;				break;			}		}		if (form.elements[i].value == "" ) {			alert("Veuillez remplir les champs correctement, merci !");			return false;			break;		}	}	return true;}function verifyFormEN(form){	for (i = 0; i < form.elements.length; i++) {		if(form.elements[i].name == "email") {			if (!verifyMail(form.elements[i])) {				alert("Wrong email, please fill in a proper email address.");				return false;				break;			}		}		if (form.elements[i].value == "" ) {			alert("Please give all informations.");			return false;			break;		}	}	return true;}/** * Website navigation  */function setTitle(par) {	document.title = par;}function setPageBackground(img_url) {	//var options = options ? options : '';	$('body').css('background-image', 'url(' + img_url + ')');	$('body').css('background-repeat', 'no-repeat');}function setLeftPageImage(img_url) {	$('#left-page-image').attr('src', img_url);}/** * Email utility (minimize spam bots) */function sendMailTo(name, company, domain) {  locationstring = 'mai' + 'lto:' + name + '@' + company + '.' + domain;  window.location.replace(locationstring);}function bold(textarea) {	document.getElementById(textarea).value += '**bold text**';}function italic(textarea) {	document.getElementById(textarea).value += '||italic text||';}function underline(textarea) {	document.getElementById(textarea).value += '__underlined text__';}function link(textarea) {	document.getElementById(textarea).value += '[[www.website.ch Text to be linked]]';}/** * Menu déroulant */function showMenu() {	var obj= document.getElementById('second-menu');	obj.style.visibility='visible';}function hideMenu() { 	var obj= document.getElementById('second-menu');	obj.style.visibility='hidden';}/** * Media player */function play(dew) {  var dewp = document.getElementById(dew);  if(dewp!=null) dewp.dewplay();}function stop(dew) {  var dewp = document.getElementById(dew);  if(dewp!=null) dewp.dewstop();}function pause(dew) {  var dewp = document.getElementById(dew);  if(dewp!=null) dewp.dewpause();}function pauseMainMP3() {	document.getElementById('headerMP3').SetVariable('player:jsPause', '');}